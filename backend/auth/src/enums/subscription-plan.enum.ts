import { TOKENS } from "../utils/tokens.utils";

export enum SubscriptionPlan {
  None = TOKENS.subscriptionPlan.none,
  Basic = TOKENS.subscriptionPlan.basic,
  Standard = TOKENS.subscriptionPlan.standard,
  Premium = TOKENS.subscriptionPlan.premium,
}
